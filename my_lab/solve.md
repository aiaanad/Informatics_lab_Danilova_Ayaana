Для подсчета выручки можно использовать команду awk, которая позволяет обрабатывать данные в столбцах. Выполните следующую команду:
```bash
grep "мармелад" sales.txt | awk '{sum += $2 * $3} END {print sum}'   
```

Result: 76400